ISBN lookup tool for Node.js
Queries the ISBNDB database for information about a book using the ISBNDB API.

This tool requires a key which can be acquired at www.isbndb.com
Once you have a key paste it into the appropriate field of the API_KEYS.json file.